# 主网参数决策草案（Phase 1.2）

> 最后更新：2026-07-04
>
> 定位：本文档是 `44-L1-MASTER-PLAN-CN.md` **Phase 1.2** 的交付物——主网经济/共识参数的**决策草案**，不是研究报告。每个参数给出：现值（注明出处）、建议主网值、理由（引用 miner-economics v3 模型的具体数字）、待外部 review 确认的开放问题。
>
> 定稿流程：本草案 → Phase 1.3 外部经济学 review（REVIEW-REQUEST.md，1–2 场）→ Phase 2 测试网实测修订 → Phase 3.5 主网参数定稿。
>
> 状态：**草案（未定稿，禁止据此实现）**

---

## 0. 阅读说明与方法

**现值的取值优先级**：协议常量（`qfc-core/crates/qfc-types/src/constants.rs`，2026-07-04 核对）> 运行时代码实际行为 > 设计文档（02/03/12）。三者不一致时**明确标注冲突**，不默认采信任何一方（冲突清单见 §6）。

**量化基础**：`miner-economics/` v3 刷新（2026-07-04，`refresh.py` + `OUTPUT.md`，基于 12 周 snapshots.jsonl）。v3 数据来自**分叉中的测试网**（12 个快照仅 5 个可用），只能视为指示性；凡结论依赖真实需求或健康链行为的，信心一律标**低**，不假装精确。

**两个贯穿性事实**：
1. **测试网从未真实触发过任何 slashing**（v3：pass rate 100%，分叉期间亦无 slash 事件记录）。§4 的全部力度建议都没有实战数据背书。
2. **测试网从未达成多节点共识**（44 号 §0）。凡"观测值 vs 理论值"的偏差（如 §2.2 的 12× 缺口），首先怀疑分叉，其次才是参数。

**信心等级定义**：高 = 协议已实现且逻辑自洽，只需确认；中 = 方向有依据但数值需外部 review；低 = 依赖尚不存在的数据（真实需求、真实攻击、健康链），当前任何数值都是占位。

---

## 1. 初始通胀率 / Emission

### 1.1 现值（协议常量，已实现）

| 项 | 值 | 出处 |
|---|---|---|
| 初始区块奖励 | 10 QFC/block | `BLOCK_REWARD = 10^19 wei`，constants.rs |
| 减半周期 | 每年 1 次，最多 4 次 | `HALVING_PERIOD_YEARS = 1`；`block_reward_for_year()` 中 `year.min(4)` |
| 奖励下限 | 0.625 QFC/block（第 4 年起） | `MIN_BLOCK_REWARD = 625_000_000_000_000_000` |
| 初始供应 | 10 亿 QFC | `INITIAL_SUPPLY` |
| 供应上限 | 20 亿 QFC | `MAX_SUPPLY` |
| 名义块时间 | 3333 ms | `BLOCK_TIME_MS` → 约 946 万块/年 |
| 年份计算方式 | **按区块高度** | `producer.rs::calculate_year()`：`block_height / blocks_per_year` |

对应年通胀率（对初始 10 亿）：Y0 ≈ 9.5% → Y1 ≈ 4.3% → Y2 ≈ 2.1% → Y3 ≈ 1.0% → Y4+ ≈ 0.5%（03 号文档表格与此一致；其"每 3 秒一块、每年约 10,512,000 块"的脚注与自身表格矛盾，见 §6 冲突 5）。

### 1.2 建议主网值：**维持现有减半表不变**

**理由（v3 具体数字）**：
- 减半表是 Gap C"条件性通过"结论的结构性基础：在刷新后基线（矿工池 4,681.69 QFC/日）且**零需求增长**下，矿工池费用占比纯靠 emission 衰减走出 29%（Y0）→ 45%（Y1）→ 62%（Y2）→ 76.6%（Y3）→ 86.7%（Y4）。链按计划自动从补贴驱动转为需求驱动，不需要治理干预。
- v2 的关键教训（MINER-ECONOMICS §4）：需求扩 10 倍对矿工收入影响甚微，**token 价格 × 矿工数**才是主导变量——调 emission 曲线解决不了真正的问题（需求为零），只会破坏已验证自洽的模型。
- 44 号放弃门（"币价 > $10 才有人跑"）未触发：最差现实格子是 n=1,000 的租用数据中心 GPU 需 $2.11，且该硬件本就不该跑这条链。

**修改方案被否决的记录**：v2 §5 路径 3（重设计 emission 让费用在 n≈100 时占主导）被 v3 否决——衰减已经内建，提前砍 Y0 补贴只会加剧"矿工熬不过 year 0"的第一存活条件（币价 < $0.05 时 RTX 3060 亏损，n=100 时保本价 $0.021）。

### 1.3 测试网 → 主网的 year-0 时钟

**建议：主网 fresh genesis 从 height 0 重新起算 year 0（即时钟重置），测试网已消耗的减半进度不带入。**

- 这实际上是**协议默认行为**：`calculate_year()` 按高度计算，主网新 genesis 高度归零，时钟自动重置。本条是确认默认，不是新增机制。
- 理由：主网矿工/验证者的 year-0 补贴期是冷启动的核心资源（v3 存活条件 1），不应被测试网烧掉；且测试网多次 reset（06-18、06-27）已多次隐式重置该时钟，从无"延续时钟"的先例或机制。
- 附带发现：`calculate_year()` 的注释写"~262,800 blocks per year"，实际算式结果约 9,462,046 块/年——**注释错误、代码正确**，主网前修注释（不影响行为）。

**信心：高**（协议已实现且模型验证自洽；重置是默认行为）。

### 1.4 开放问题（外部 review）

- **OQ-1**：按高度计年，实际块时间若持续偏离 3333 ms（测试网分叉期即如此），减半的墙钟时点会漂移。可接受，还是应改为按 genesis 时间戳计年？（Bitcoin 按高度，先例支持现状。）
- **OQ-2**：`reward_multiplier` 动态调整（producer.rs，随质押率/拥堵缩放奖励）叠加在减半表上，v3 模型**未建模**该乘数。主网前需明确其取值范围并补入模型，否则实际通胀可偏离本节表格。

---

## 2. 奖励分配

### 2.1 PoC 贡献权重

**现值**（constants.rs `WEIGHT_*`，与 02 号文档一致）：质押 30% / 计算 20% / 在线 15% / 准确率 15% / 网络 10% / 存储 5% / 信誉 5%。

**建议主网值：维持不变。**

理由：没有任何数据支持修改——v3 明确指出"n=2 时两矿工恰好 50/50 均分，1/n 假设在 n>2 完全未经检验"，PoC 权重在实践中的分配效果同样未被观测过。在零观测数据下调整多维权重是伪精确。Phase 2 招到 ≥10 外部矿工后用真实分布复核。

**信心：中**（维持的理由是"无证据支持修改"，不是"有证据支持现值"）。

### 2.2 区块奖励分配与矿工池占比

**现值**（constants.rs + producer.rs，已实现）：
- 区块奖励：生产者 60% / 投票者 25% / **推理矿工 15%**（`PRODUCER/VOTERS/INFERENCE_MINERS_REWARD_PERCENT`；矿工按块内 proof 的 FLOPS 占比分；无 proof 时 15% 按 70/30 回流生产者/投票者）。
- 注意：02 号文档 §区块奖励分配与 03 号文档收益公式仍写 70/30 且**无矿工份额**——已过时，见 §6 冲突 1。
- 15% 是名义值，producer.rs 中 `adjustments.inference_miner_percent` 可动态调整。

**v3 的 12× 缺口——正面回答"主网目标值是多少"**：
- 理论：15% × 10 QFC/block × 946 万块/年 ⇒ 矿工通胀池 ≈ **38,884 QFC/日**（OUTPUT.md）。
- 观测（06-28 快照）：通胀切片仅 **3,323.69 QFC/日**，约为理论值的 **1/11.7**。
- **归因判断：这是分叉链出块紊乱的症状，不是参数设计错误。** v3 原文："If the consensus fix lands and block production normalizes, the year-0 pool could be substantially larger than modeled here."

**建议主网目标值：维持名义 15%；将"实测矿工池 ≥ 理论值的 80%（约 31,000 QFC/日 @ Y0）"列为 Phase 0 出口门之后的验收指标**，即共识修复 + 最终 reset 后，用首月快照重跑 `refresh.py` 验证缺口消失。若健康链上缺口仍 >2×，说明分配路径有实现级 bug（而非共识症状），届时先修 bug 再议参数。**在健康链数据出来前，不据分叉数据调整 15% 本身。**

**信心：中**（15% 的绝对水平缺乏外部对照——这是 REVIEW-REQUEST 的核心问题之一；12× 缺口的归因虽有依据但未被健康链证实）。

### 2.3 Gas 费分配——推理矿工 0% 的问题，正面处理

**现值**（constants.rs `FEE_*`，producer.rs 已实现）：生产者 47% / 投票者 28% / 销毁 20% / 国库 5%，**推理矿工 0%**。（03 号文档写 50/30/20 无国库——已过时，见 §6 冲突 2。）

**建议主网值：维持 0%，但作为显式决策记录在案，并列为外部 review 必答题。**

理由：
- 推理矿工已有两条专属收入流：区块奖励 15% 池（§2.2）+ 推理任务费（`INFERENCE_FEE_*`：矿工 70% / 验证者 10% / 销毁 20%）。Gas 费来自转账/DEX/NFT 等与推理无因果关系的流量，v3 §7 明确将其排除在矿工模型外（"they don't pay miners — they burn or go to validators"）。
- 给矿工加 gas 切片必然稀释验证者/销毁份额，而主网启动期验证者激励同样脆弱（12 号文档要求官方先扛稳定性）。
- v3 模型显示矿工经济的瓶颈不在收入切片的划法，在**需求本身为零**（`unique_submitters_last_100 = 0`）。动 gas 分配是错误层面的修补。

**反方论点（如实记录）**：长期看（Y4+）emission 池萎缩到 207.7 QFC/日，若届时推理任务费仍未起量而 gas 流量起量了，矿工在费用主导时代将无结构性收入。若外部 review 认为该情形概率不可忽略，备选方案是"国库 5% 中划出部分定向补贴矿工"，不动 47/28/20。

**附带口径问题（OQ-3）**：v3 模型按 0.1 QFC/task **全额**计入矿工费用收入（1,358 = 13,580 × 0.1），但协议常量 `INFERENCE_FEE_MINER_PERCENT = 70`。若 RPC 口径实为 70%，v3 的费用切片高估约 1.43×（29% 费用占比实为约 22%）。健康链上核对 `qfc_getMinerEarnings` 口径后修正模型。见 §6 冲突 6。

**信心：中**（维持 0% 的逻辑自洽，但依赖"推理费终将起量"这一未验证假设）。

---

## 3. 质押门槛

### 3.1 Validator

**现值**：

| 项 | 值 | 出处 |
|---|---|---|
| 最低质押 | 10,000 QFC | `MIN_VALIDATOR_STAKE = 10^22 wei`；03、12 号文档一致 |
| 协议验证者上限 | 1,000 | `MAX_ACTIVE_VALIDATORS` |
| 12 号文档启动期上限 | 50（政策层） | 12 号 §参数建议 `max_validators = 50` |
| 单验证者质押占比上限 | 1% | `MAX_VALIDATOR_STAKE_PERCENT` |
| 12 号文档实体权重上限 | 20%（政策层） | `max_entity_weight = 0.20` |
| 解锁等待期 | 7 天 | `UNSTAKE_DELAY_SECS` |
| 最低委托 | 100 QFC | `MIN_DELEGATION` |

**建议主网值**：
- 最低质押 **维持 10,000 QFC**。三处文档与常量完全一致，是本草案中最无争议的参数。锚定物：12 号文档的风险模型基于贡献权重而非质押绝对值，门槛只需过滤 sybil、不承担防控职能。
- 启动期验证者上限 **50（政策层准入），协议上限维持 1,000 不动**——采纳 12 号文档阶段 A 方案（官方权重 25–30%、每周批次准入 ≤5）。
- **实体权重上限采用 12 号文档的 20%（政策层）；`MAX_VALIDATOR_STAKE_PERCENT = 1%` 必须在主网前修改**——它与 50 验证者的启动集**数学不相容**：50 个验证者各 ≤ 总质押 1%，合计 ≤ 50% < 100%，无解。该常量只在验证者数 ≥ 100 时可满足。见 §6 冲突 4。**建议改为 20%**（与 12 号文档实体上限对齐，仍远低于 1/3 活性阈值），网络成熟后经治理下调。

**信心**：最低质押 **高**；上限体系 **中**（1% → 20% 的修改幅度需外部 review 确认，且需检查该常量当前是否真的被 enforce——若从未生效，测试网 3 验证者早已违反它）。

### 3.2 Inference Miner（与 validator 分开）

**现值**：
- **推理矿工注册无质押门槛**——`qfc-ai-coordinator/src/registry.rs` 及关联代码中不存在 miner 最低质押常量；注册即入。
- 训练任务（AI-V3）例外：动态质押下限 = `slash_multiple(40) × per_step_reward × steps_per_epoch`（`training.rs`，ADR-0008），这是"作弊经济上必亏"的安全下限，不是准入门槛。

**建议主网值：推理矿工维持零质押门槛（注册免押）；训练矿工维持动态下限公式不变。**

理由（v3 数字直接决定方向）：
- v3 存活条件 1："矿工必须熬过 year 0"，而 Y0 保本表（OUTPUT.md）显示只有 $0 边际成本设备普遍保本：闲置笔记本任何 n 都保本、闲置 VPS 在 n=1,000 也只需 $0.035，而 RTX 3060 在 n=500 就需要 **$0.105**、n=1,000 需 **$0.211**。目标人群是"顺手挂机"的低成本设备主，**任何有感的质押门槛都会精准劝退唯一保本的群体**。
- v3 结论原文："below ~$0.02/QFC, only $0-marginal hardware should be onboarded past n≈100"。门槛必须为零或接近零。
- 防作弊不靠准入质押：spot-check（5% 抽查）+ InvalidInference 罚没（§4）+ 训练侧 40× 动态下限已覆盖。sybil 灌水矿工的收益被 1/n 摊薄机制自然抑制（多注册身份不增加 FLOPS 份额）。

**开放问题（OQ-4）**：零门槛下，恶意矿工被 slash 的对象是什么？InvalidInference 按"5% of stake"执行，而注册免押的矿工 stake 可能为 0 ⇒ 罚没额为 0，只剩 6h jail。**这是真实的机制空洞**：要么给推理矿工设一个小额保证金（如 10–100 QFC，量级以不劝退 VPS 玩家为准），要么把 InvalidInference 改成训练侧那样的绝对额罚没。提交外部 review 定夺。

**信心**：零/极低门槛的**方向**为 **高**（v3 数据直接支撑）；具体保证金数值为 **低**（依赖对真实作弊行为的观测，目前为零）。

---

## 4. Slashing

### 4.1 现状盘点——三处定义互相冲突，且从未真实触发

**测试网至今没有发生过一次真实 slashing**（v3：pass rate 100%；分叉三个月也未产生任何 slash 记录——分叉本身反而说明双签/错误投票检测从未在实战中亮过）。以下所有力度都是纸面值。

| 触发条件 | constants.rs（`SLASH_*`） | 运行时实际（`qfc-node/src/sync.rs` 证据分发路径，硬编码） | 02 号文档 |
|---|---|---|---|
| DoubleSign | 50% | **10% + jail 24h** | 50% + 永久禁止 |
| InvalidBlock | 10% | **5% + jail 12h** | 10% + 7 天 |
| Censorship | 5% | **3% + jail 6h** | 5% + 3 天 |
| Offline | 1% | 1% + jail 1h | 1% + 1 天 |
| FalseVote | 2% | 2% + jail 2h | 2% + 1 天 |
| InvalidInference | （无常量） | 5% + jail 6h | （02 未覆盖；qfc-core CLAUDE.md Phase 7 记录同值） |
| InvalidTraining（AI-V3） | （无常量） | 绝对额：`slash_multiple × per_step_reward`，默认 **40×**（`qfc-ps/lib.rs PsConfig::default`，ADR-0008/0009）；仅走 A5 绝对额路径，sync.rs 显式拒绝其进入百分比路径 | （未覆盖） |

**更糟的是 DoubleSign 存在双路径分裂**：`qfc-consensus/engine.rs` 的 `DoubleSignEvidence` 处理（带密码学证据）按 `SLASH_DOUBLE_SIGN_PERCENT` = 50% + **永久 jail** 执行；而 `sync.rs` 的泛用 `SlashingEvidence` gossip 路径对同一罪名硬编码 10% + 24h。同一行为两种刑罚，取决于证据走哪条管道。见 §6 冲突 3。

### 4.2 建议主网值

**第一原则：先修一致性，再谈力度。** 主网前必须把 slash 参数全部收敛到 constants.rs 单一出处，运行时路径禁止硬编码（InvalidInference 也应补常量）。

建议力度（以 constants.rs / 02 号文档一侧为基准，即**从严**）：

| 触发条件 | 建议罚没 | 建议 jail | 论证 |
|---|---|---|---|
| DoubleSign | **50%** | **永久（可治理申诉）** | 双签是共识安全的底线罪；10%/24h 的 sync.js 路径对 10,000 QFC 质押者只罚 1,000 QFC，不足以威慑分叉攻击。与 engine.rs 已实现行为对齐，改动最小 |
| InvalidBlock | 10% | 7 天 | 维持 constants/02 值；无数据支持调整 |
| Censorship | 5% | 3 天 | 同上；注：审查行为的**检测机制**尚不存在，此参数主网初期实际不可执行，如实标注 |
| Offline | **1%，但含警告缓冲** | 24h | 主网初期节点运维不成熟（我方自己的测试网三个月不收敛），无缓冲的立即罚没会误伤诚实小验证者。建议首次 offline 仅 jail 不罚没，重复才罚 1% |
| FalseVote | 2% | 24h | 维持 |
| InvalidInference | 5% | 6h | 维持已实现值；但**先解决 §3.2 OQ-4 的零质押空洞**，否则该条对免押矿工是空文 |
| InvalidTraining | **40 × per_step_reward（绝对额），维持** | 6h（沿用 v2.x 惯例，`training_verification.rs`） | ADR-0008 的经济论证成立：抽查率 5% 下，作弊期望收益为负需要倍数 ≥ 1/0.05 = 20，40× 提供 2× 安全边际。`per_step_reward` 的主网值未定（OQ-7） |

**信心：低**（除 InvalidTraining 的 40× 有明确经济推导为**中**外，其余全部力度从未被真实触发检验过——这必须在 REVIEW-REQUEST 中如实告知外部 reviewer）。

### 4.3 开放问题

- **OQ-5**：Offline 的**判定阈值**（多久无 heartbeat 算 offline）未见于常量层，主网前需定义并写入 constants.rs。
- **OQ-6**：永久 jail 的治理解禁流程（03 号文档治理章节有提案类型但未覆盖"申诉"）。
- **OQ-7**：`per_step_reward` 主网值未定，InvalidTraining 的实际威慑力和训练矿工质押下限都随它缩放。
- **OQ-8**：被罚没资金去向——02/03 号文档说销毁，`settlement.rs` 的绝对额路径是否同样销毁需在节点集成（Phase 3.2）时确认。

---

## 5. 一页汇总表

信心：高 = 只需确认；中 = 方向有据、数值待 review；低 = 缺真实数据，占位值。

| # | 参数 | 现值（出处） | 建议主网值 | 信心 | 依赖哪场外部 review |
|---|------|------------|-----------|------|--------------------|
| 1 | 初始区块奖励 | 10 QFC/block（constants.rs） | 维持 | 高 | Phase 1.3 经济 review（确认性） |
| 2 | 减半表 | 每年减半 ×4，floor 0.625（constants.rs） | 维持 | 高 | 同上——ramp 数学是 REVIEW-REQUEST 核心问题 |
| 3 | year-0 时钟 | 按高度计年（producer.rs） | 主网 genesis 重置，测试网进度不带入 | 高 | 无需外部（协议默认行为） |
| 4 | 供应 | 10 亿初始 / 20 亿上限（constants.rs） | 维持 | 高 | Phase 1.3（确认性） |
| 5 | PoC 权重 | 30/20/15/15/10/5/5（constants.rs = 02 号） | 维持 | 中 | Phase 2 后用 ≥10 矿工实测复核 |
| 6 | 区块奖励分配 | 60/25/15 生产者/投票者/矿工（constants.rs；02/03 号文档过时） | 维持 15% 矿工池；健康链首月验证实测池 ≥ 理论 80%（约 31,000 QFC/日） | 中 | Phase 1.3 +Phase 0 后重跑 refresh.py |
| 7 | Gas 费分配 | 47/28/20/5，矿工 0%（constants.rs；03 号过时） | 维持 0%（显式决策）；备选：国库切片定向补贴 | 中 | Phase 1.3 必答题 |
| 8 | 推理费分配 | 70/10/20 矿工/验证者/销毁（constants.rs） | 维持；先核对 v3 模型口径（100% vs 70%） | 中 | 健康链 RPC 核对 |
| 9 | Validator 最低质押 | 10,000 QFC（constants.rs = 03 = 12 号） | 维持 | 高 | Phase 1.3（确认性） |
| 10 | 启动期验证者上限 | 协议 1,000 / 政策 50（12 号） | 政策 50，按 12 号阶段 A→C 放宽 | 中 | Phase 1.3 |
| 11 | 单验证者质押占比上限 | 1%（constants.rs）——与 50 验证者启动集数学不相容 | **改 20%**，对齐 12 号实体上限 | 中 | Phase 1.3 必答题 |
| 12 | 委托最低额 / 解锁期 | 100 QFC / 7 天（constants.rs） | 维持 | 高 | 无 |
| 13 | Miner 质押门槛 | 无（registry.rs 免押注册） | 维持零门槛；小额保证金（10–100 QFC）待定 | 方向高 / 数值低 | Phase 1.3（OQ-4 空洞） |
| 14 | 训练质押下限 | 40 × r × steps/epoch 动态（training.rs） | 维持公式；r 主网值未定 | 中 | Phase 1.3 + Phase 3.2 |
| 15 | DoubleSign | 50%（constants）vs 10%/24h（sync.rs）vs 50%+永久（02 号） | **50% + 永久 jail**，收敛单一路径 | 低 | Phase 1.3 + 安全审计（Phase 3.1） |
| 16 | InvalidBlock | 10% vs 5%/12h（同上三方不一致） | 10% + 7 天 | 低 | 同上 |
| 17 | Censorship | 5% vs 3%/6h | 5% + 3 天（检测机制缺失，暂不可执行） | 低 | 同上 |
| 18 | Offline | 1%（三方一致，jail 期不一致） | 1% + 24h，首犯只 jail 不罚 | 低 | 同上 |
| 19 | FalseVote | 2% | 2% + 24h | 低 | 同上 |
| 20 | InvalidInference | 5%/6h（仅 sync.rs 硬编码，无常量） | 维持，入 constants.rs；先补零质押空洞 | 低 | 同上 |
| 21 | InvalidTraining | 40 ×（qfc-ps 默认，ADR-0008） | 维持 40× | 中 | Phase 1.3（经济推导可独立检验） |

---

## 6. 冲突清单（文档 / 常量 / 运行时不一致，须在主网前消除）

1. **区块奖励分配**：constants.rs + producer.rs 实现 60/25/15（含矿工池）；02 号 §区块奖励分配与 03 号收益公式写 70/30、无矿工份额。02/03 过时（70/30 现在只是"无 proof 时矿工池的回流比例"）。→ 修订 02/03。
2. **Gas 费分配**：constants.rs 47/28/20/5（含国库 5%）；03 号写 50/30/20、无国库。→ 修订 03。
3. **Slashing 三方分裂**：constants.rs `SLASH_*`（50/10/5/1/2）从未被证据分发路径使用——`sync.rs` 硬编码另一套（10/5/3/1/2 + 小时级 jail）；02 号又是第三套 jail 期（天级/永久）。DoubleSign 更存在双路径（engine.rs 50%+永久 vs sync.rs 10%+24h）。→ 主网前收敛到 constants.rs 单一出处（§4.2）。
4. **`MAX_VALIDATOR_STAKE_PERCENT = 1%` 与 12 号启动方案数学不相容**：50 个验证者各 ≤1% ⇒ 总质押 ≤50%，无解。且测试网 3 验证者若该约束生效早已违反——需确认它是否根本未被 enforce。→ 改 20% 或确认为死代码后重设。
5. **03 号内部矛盾**：通胀表按约 946 万块/年（3.333 s）计算，脚注却写"每 3 秒一个区块，每年约 10,512,000 个区块"。→ 修脚注。
6. **推理费口径**：v3 模型将 0.1 QFC/task 100% 计给矿工；协议 `INFERENCE_FEE_MINER_PERCENT = 70`。若 70% 为实，v3 费用切片高估约 1.43×，Y0 费用占比 29% → 约 22%（各年费用占比曲线同步下移，但§1.2 的结构性结论不变）。→ 健康链核对 `qfc_getMinerEarnings` 口径后修模型。
7. **`calculate_year()` 注释错误**："~262,800 blocks per year" 应为约 9,462,046。代码正确，仅注释误导。→ 修注释。

---

## 7. 外部 review 开放问题汇总（供 REVIEW-REQUEST.md 引用）

| # | 问题 | 所在节 |
|---|------|--------|
| OQ-1 | 减半按高度计年，块时间漂移导致减半墙钟时点漂移，可否接受？ | §1.4 |
| OQ-2 | `reward_multiplier` 动态乘数未入 v3 模型，取值范围需界定 | §1.4 |
| OQ-3 | 推理费矿工口径：100%（模型假设）还是 70%（协议常量）？ | §2.3 |
| OQ-4 | 免押推理矿工使 InvalidInference 的"5% of stake"罚没为零——小额保证金还是改绝对额罚没？ | §3.2 |
| OQ-5 | Offline 判定阈值未定义 | §4.3 |
| OQ-6 | 永久 jail 的治理申诉/解禁流程 | §4.3 |
| OQ-7 | `per_step_reward` 主网值（决定训练罚没绝对额与质押下限） | §4.3 |
| OQ-8 | 罚没资金去向（销毁 vs 国库）在绝对额路径上未确认 | §4.3 |
| 核心题 | 15% 矿工池 + 减半 ramp 在"n≈100–500、币价 $0.02–0.10"区间能否让矿工存活到费用主导期？（v3 保本表为论据） | §2.2 / REVIEW-REQUEST |

**依赖真实需求的一切数值**（费用占比的绝对轨迹、矿工池主网目标绝对额、保证金数额）在需求为零（`unique_submitters_last_100 = 0`）的今天都无法负责任地给出，本草案一律标信心=低并交由 Phase 2 实测数据决定，不制造伪精确。

---

*本草案由 Phase 1.1 v3 模型（`miner-economics/refresh.py`，2026-07-04）与 qfc-core 协议常量（同日核对）推导。下一步：Phase 1.3 ——把本草案 + MINER-ECONOMICS v3 放到 1–2 位 PoS/DePIN 经济学从业者面前。*
